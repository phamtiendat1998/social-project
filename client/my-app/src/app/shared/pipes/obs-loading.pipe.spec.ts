import { ObsLoadingPipe } from './obs-loading.pipe';

describe('ObsLoadingPipe', () => {
  it('create an instance', () => {
    const pipe = new ObsLoadingPipe();
    expect(pipe).toBeTruthy();
  });
});
